import 'position.dart';
import 'package:chess/chess.dart';

class ChessGame {
  final Chess chess = Chess();

  // List<Position> getLegalMoves(Position position) {
  //   final moves = chess.generate_moves()
  //   return moves.map((move) {
  //     return Position(move[0], move[1]); // Convert to your Position format
  //   }).toList();
  // }
  //
  // void movePiece(Position from, Position to) {
  //   chess.move(from.toString(), to.toString()); // Adjust based on your Position format
  // }
}
